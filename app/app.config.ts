export default defineAppConfig({
    ui: {
        colors: {
            primary: 'black',
            neutral: 'zinc'
        },

        main: {
            base: 'min-h-[calc(100vh-var(--ui-header-height))] mx-auto max-w-xl px-4'
        }
    }
})
